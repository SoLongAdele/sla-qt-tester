# PyWebView Desktop App 依赖

# 核心依赖
pywebview>=5.3.1
openai
python-dotenv
requests>=2.32.3
watchdog>=6.0.0

# 视觉测试依赖
pyautogui>=0.9.54
opencv-python>=4.8.0
numpy>=1.24.0
pygetwindow>=0.0.9; sys_platform == 'win32'

# 可选：更好的性能和功能
# macOS 推荐
pyobjc>=10.0; sys_platform == 'darwin'

# Windows 推荐
pywin32>=306; sys_platform == 'win32'

# Linux 推荐 (需要系统安装 GTK)
# PyGObject>=3.46.0; sys_platform == 'linux'
